#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,i,value;
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++){
        scanf("%d",&a[i]);
    }
    scanf("%d",&value);
    int j=0;
    for(i=0;i<n;i++){
        if(a[i]!=value){
            a[j++]=a[i];
        }
    }
    for(i=0;i<j;i++){
        printf("%d ",a[i]);
    }
    

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
